# Minimum floors rather than pins: the point is to run on whatever the GPU box
# already has. On a CUDA machine install torch from the index matching its driver
# (https://pytorch.org/get-started/locally/) before installing this file.
torch>=2.4
transformers>=4.40
safetensors>=0.4
numpy>=1.26

# HTTP server (nanoserve.server)
fastapi>=0.110
uvicorn>=0.27

# tests only
pytest>=8.0
